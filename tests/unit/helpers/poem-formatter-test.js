import { poemFormatter } from 'poetry-pictures/helpers/poem-formatter';
import { module, test } from 'qunit';

module('Unit | Helper | poem formatter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = poemFormatter([42]);
  assert.ok(result);
});
